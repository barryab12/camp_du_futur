<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="mon_module.list">
      <field name="name">mon_module list</field>
      <field name="model">mon_module.mon_module</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="mon_module.action_window">
      <field name="name">mon_module window</field>
      <field name="res_model">mon_module.mon_module</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="mon_module.action_server">
      <field name="name">mon_module server</field>
      <field name="model_id" ref="model_mon_module_mon_module"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="mon_module" id="mon_module.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="mon_module.menu_1" parent="mon_module.menu_root"/>
    <menuitem name="Menu 2" id="mon_module.menu_2" parent="mon_module.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="mon_module.menu_1_list" parent="mon_module.menu_1"
              action="mon_module.action_window"/>
    <menuitem name="Server to list" id="mon_module" parent="mon_module.menu_2"
              action="mon_module.action_server"/>
    -->
  </data>
</odoo>